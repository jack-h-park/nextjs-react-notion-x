# Langfuse → PostHog event mapping (archived)

Signal mapping now lives inside `docs/telemetry/alerting-contract.md`, which covers both the alert contract and the canonical PostHog events/properties that Step 3 consumes. Please update that file whenever you change event names or properties.
