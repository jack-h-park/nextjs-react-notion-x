# Langfuse alert specification (archived)

This file has been consolidated into `alerting-contract.md`, which now couples the alert intent with the canonical PostHog signal mapping. For the current spec, event taxonomy, and severity matrices, read `docs/telemetry/alerting-contract.md`.
